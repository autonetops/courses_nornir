# O grupo carrega o que os hosts de uma plataforma compartilham.
# `platform` e lido pelo plugin de conexao (netmiko: vira o device_type).
eos:
  platform: arista_eos       # Arista cEOS
ios:
  platform: cisco_ios        # Cisco IOL roda IOS classico
