# nornir-lab/requirements.txt
# Stack fixado do curso — casa com a imagem do sandbox e a do CI.
# O pipeline instala exatamente estas versoes (pip install -r requirements.txt)
# antes dos jobs de test e dry-run. O ruff NAO esta aqui: nao e dependencia de
# runtime; o job de lint o instala a parte.
nornir==3.5.0
nornir-netmiko==1.0.1
nornir-napalm==0.5.0
nornir-scrapli==2025.1.30
nornir-jinja2==0.2.0
nornir-utils==0.2.0
infrahub-sdk==1.22.1
nornir-infrahub==1.1.2
prefect==3.7.7
pytest==8.3.4
